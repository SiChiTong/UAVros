增量式PID太差了，实际效果完全累加的不对，因为error的更新和command_v的PID解算是两个线程，它们的频率不一样，因此靠增量式累加获得的输出有误差且不断累积。

对于P控制，不如用位置式PID，out=Kp*error。这样error和out更新频率不同，对结果影响也不大

ros这种多线程的对于累加很不友好，误差会越来越大。特别是对于纯粹的积分累加（没有比例P项），如上次Prometheus KCF里用速度积分求位置，误差越来越大。

但对于不要求积分结果准确，只要求有积分项的地方（如PI控制，稳态时error=0，P、I项均为0了，靠积分项已经累积了速度），累加式的积分还是管用的吧也许。
